# Example Configure VM with Ansible in Vagrant

## Execute
Make sure you in `example/` directory. You can custom this playbook.yaml to whatever you want. :D
```
vagrant up --provision
```

See also: https://www.lukmanlab.com/tag/vagrant/
